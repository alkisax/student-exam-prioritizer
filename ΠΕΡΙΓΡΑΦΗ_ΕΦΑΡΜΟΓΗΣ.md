# Student Exam Prioritizer — Περιγραφή εφαρμογής

## 1. Μη τεχνική περιγραφή

### Σκοπός

Το **Student Exam Prioritizer** είναι ένα εργαλείο υποστήριξης εκπαιδευτικών για τη δημιουργία μιας αιτιολογημένης σειράς προτεραιότητας μαθητών προς εξέταση. Στόχος του είναι να βοηθά τον εκπαιδευτικό να εντοπίζει γρήγορα ποιοι μαθητές:

- δεν έχουν εξεταστεί ακόμη,
- δεν έχουν βαθμό σε ένα ή περισσότερα κριτήρια αξιολόγησης,
- έχουν εξεταστεί λιγότερες φορές από τους υπόλοιπους,
- ή χρειάζονται βελτίωση λόγω χαμηλού τελικού βαθμού.

Η εφαρμογή δεν αντικαθιστά την κρίση του εκπαιδευτικού. Η παραγόμενη κατάταξη λειτουργεί ως πρόταση και ως βοήθημα για μια πιο συστηματική και ισόρροπη διαδικασία εξέτασης.

### Σε ποιους απευθύνεται

Η κύρια ομάδα χρηστών είναι οι εκπαιδευτικοί που διατηρούν βαθμολογίες μαθητών σε αρχεία Excel και θέλουν να οργανώνουν την εξέταση ανά μάθημα και σχολικό έτος.

### Βασική ροή χρήσης

1. Ο εκπαιδευτικός συνδέεται με όνομα χρήστη και κωδικό πρόσβασης.
2. Επιλέγει ένα υπάρχον σχολικό έτος και μάθημα ή δημιουργεί νέο μάθημα.
3. Εισάγει ένα αρχείο Excel με τα ονόματα και τις βαθμολογίες των μαθητών.
4. Αποθηκεύει την τρέχουσα εικόνα των δεδομένων (snapshot).
5. Ζητά από την εφαρμογή να υπολογίσει τη σειρά προτεραιότητας.
6. Βλέπει μια ταξινομημένη λίστα με τη θέση, τον βαθμό, τον εκτιμώμενο αριθμό εξετάσεων, τη βαθμολογία προτεραιότητας και σύντομες αιτιολογήσεις για κάθε μαθητή.

### Μορφή του αρχείου Excel

Η εφαρμογή διαβάζει το πρώτο φύλλο του αρχείου και αναζητά:

- τη στήλη **`Alumno/a`** για το όνομα του μαθητή,
- τη στήλη **`Nota`** για τον τελικό βαθμό,
- στήλες με τίτλο της μορφής **`αριθμός.αριθμός`** (π.χ. `1.1`, `2.3`) ως κριτήρια αξιολόγησης.

Υποστηρίζονται αρχεία `.xlsx` και `.xls`. Κενές ή μη έγκυρες αριθμητικές τιμές θεωρούνται απουσία βαθμού, ενώ γίνεται δεκτό τόσο το κόμμα όσο και η τελεία ως δεκαδικό σύμβολο.

### Πώς προκύπτει η προτεραιότητα

Η εφαρμογή αποδίδει σε κάθε μαθητή μια βαθμολογία προτεραιότητας:

- **+1000 μονάδες** αν δεν έχει εξεταστεί καθόλου ή δεν έχει κανέναν βαθμό κριτηρίου,
- **+100 μονάδες** για κάθε κριτήριο χωρίς βαθμό,
- **−10 μονάδες** για κάθε εκτιμώμενη εξέταση που έχει ήδη πραγματοποιηθεί,
- επιπλέον μονάδες όταν ο τελικός βαθμός είναι κάτω από 5: **`(5 − βαθμός) × 20`**.

Οι μαθητές εμφανίζονται από τη μεγαλύτερη προς τη μικρότερη βαθμολογία προτεραιότητας. Σε ισοβαθμία η μεταξύ τους σειρά επιλέγεται τυχαία, ώστε να μην ευνοείται σταθερά κάποιος μαθητής.

### Ιστορικό εξετάσεων μέσω snapshots

Σε κάθε αποθήκευση, η εφαρμογή συγκρίνει τα νέα κριτήρια ενός μαθητή με τα προηγούμενα. Αν έχει αλλάξει τουλάχιστον ένας βαθμός κριτηρίου, θεωρεί ότι πραγματοποιήθηκε μία νέα εξέταση και αυξάνει τον εκτιμώμενο αριθμό εξετάσεων κατά ένα. Πολλές αλλαγές στο ίδιο αρχείο μετρούν συνολικά ως μία εξέταση.

Η αντιστοίχιση μαθητών γίνεται με βάση το όνομα, χωρίς διάκριση πεζών/κεφαλαίων και χωρίς τα κενά στην αρχή ή στο τέλος. Επομένως, η σταθερή γραφή των ονομάτων είναι σημαντική για τη σωστή συνέχεια του ιστορικού.

### Προστασία και διαχωρισμός δεδομένων

Κάθε μάθημα συνδέεται με τον λογαριασμό του εκπαιδευτικού που το δημιούργησε. Οι λειτουργίες μαθημάτων απαιτούν ενεργή σύνδεση και το backend περιορίζει την ανάγνωση ή μεταβολή μαθημάτων στον αντίστοιχο εκπαιδευτικό.

### Σημαντικές παρατηρήσεις της τρέχουσας έκδοσης

- Η κατάταξη αποτελεί βοηθητική ένδειξη και όχι αυτόματη παιδαγωγική απόφαση.
- Ο «αριθμός εξετάσεων» είναι εκτίμηση που βασίζεται στις αλλαγές των βαθμών ανά snapshot, όχι ανεξάρτητο παρουσιολόγιο εξετάσεων.
- Ένα snapshot αντικαθιστά την προηγούμενη τρέχουσα λίστα μαθητών του μαθήματος· δεν αποθηκεύεται ξεχωριστή έκδοση κάθε εισαγωγής.
- Η διεπαφή σύνδεσης είναι διαθέσιμη, ενώ η δημόσια εγγραφή χρήστη υποστηρίζεται από το backend αλλά δεν εκτίθεται αυτή τη στιγμή στις ενεργές διαδρομές της διεπαφής.
- Η διαχείριση χρηστών και ρόλων υποστηρίζεται από το API, αλλά τα σχετικά dashboard components δεν είναι συνδεδεμένα στην κύρια πλοήγηση.

---

## 2. Τεχνική περιγραφή

### Αρχιτεκτονική

Η εφαρμογή ακολουθεί αρχιτεκτονική client–server:

```text
Browser / React SPA
       │ HTTP + JSON / Bearer JWT
       ▼
Express REST API
       │ Mongoose
       ▼
MongoDB
```

Το repository χωρίζεται σε δύο ανεξάρτητα TypeScript projects:

- **`frontend/`**: Single Page Application (SPA),
- **`backend/`**: REST API και πρόσβαση στη βάση δεδομένων.

### Frontend

#### Τεχνολογίες

- React 19 και TypeScript
- Vite για development server και production build
- React Router για δρομολόγηση
- Tailwind CSS και Material UI για τη διεπαφή
- Axios και Fetch API για HTTP αιτήματα
- SheetJS (`xlsx`) για ανάγνωση Excel στον browser
- `jwt-decode` για ανάκτηση των στοιχείων χρήστη από το token

#### Διαδρομές

| Διαδρομή | Πρόσβαση | Περιγραφή |
|---|---|---|
| `/` | Δημόσια | Αρχική σελίδα |
| `/login` | Δημόσια | Φόρμα σύνδεσης |
| `/student-prioritizer` | Προστατευμένη | Διαχείριση μαθημάτων, Excel snapshots και κατάταξη |

Η προστατευμένη διαδρομή εμφανίζεται μόνο όταν το `UserAuthContext` έχει αναγνωρίσει έγκυρο και μη ληγμένο JWT. Το token φυλάσσεται στο `localStorage` και αποστέλλεται ως `Authorization: Bearer <token>`.

#### Επεξεργασία Excel

Η ανάγνωση γίνεται εξ ολοκλήρου στον browser. Το frontend:

1. φορτώνει το πρώτο worksheet,
2. εντοπίζει δυναμικά τη γραμμή επικεφαλίδων μέσω του `Alumno/a`,
3. μετατρέπει τις γραμμές σε αντικείμενα μαθητών,
4. στέλνει στο API μόνο τα δομημένα δεδομένα JSON — όχι το ίδιο το αρχείο Excel.

Η σελίδα προτεραιοποίησης χρησιμοποιεί σήμερα σταθερό API URL (`http://localhost:3019/api/courses`), ενώ η σύνδεση χρησιμοποιεί το `VITE_BACKEND_URL` με fallback το `http://localhost:3019`.

### Backend

#### Τεχνολογίες και δομή

- Node.js, Express 5 και TypeScript
- MongoDB με Mongoose
- JWT για authentication
- bcrypt για hashing και επαλήθευση κωδικών
- Zod για validation των δεδομένων authentication
- Express Rate Limit σε επιλεγμένα endpoints
- CORS και κεντρικός χειρισμός σφαλμάτων

Το backend οργανώνεται σε routes, controllers, services, DAOs και Mongoose models. Οι controllers χειρίζονται HTTP αιτήματα/απαντήσεις, τα services περιέχουν τους επιχειρησιακούς κανόνες και τα DAOs απομονώνουν την πρόσβαση στη MongoDB.

#### Authentication και authorization

- Το login επαληθεύει τον κωδικό μέσω bcrypt και επιστρέφει JWT διάρκειας μίας ώρας.
- Τα προστατευμένα endpoints απαιτούν Bearer token.
- Υπάρχουν ρόλοι `MEMBER` και `ADMIN`.
- Οι διαδρομές διαχείρισης χρηστών απαιτούν `ADMIN`, με εξαίρεση την ενημέρωση βασικών πεδίων χρήστη, η οποία απαιτεί μόνο authentication.
- Η δημιουργία, ανάγνωση, ενημέρωση, διαγραφή και προτεραιοποίηση μαθήματος φιλτράρεται με `teacherId` από το JWT.

#### Κύρια endpoints

| Μέθοδος | Endpoint | Περιγραφή |
|---|---|---|
| `GET` | `/api/ping` | Απλός έλεγχος απόκρισης |
| `GET` | `/health` | Health check |
| `POST` | `/api/auth/login` | Σύνδεση |
| `POST` | `/api/auth/register` | Δημόσια εγγραφή |
| `POST` | `/api/auth/refresh` | Ανανέωση JWT |
| `GET` | `/api/courses` | Μαθήματα συνδεδεμένου εκπαιδευτικού |
| `POST` | `/api/courses/snapshot` | Δημιουργία μαθήματος ή αποθήκευση νέας εικόνας |
| `GET` | `/api/courses/:id` | Ανάκτηση μαθήματος |
| `PUT` | `/api/courses/:id` | Ενημέρωση μαθήματος |
| `DELETE` | `/api/courses/:id` | Διαγραφή μαθήματος |
| `GET` | `/api/courses/:id/prioritize` | Υπολογισμός κατάταξης |
| `GET/POST/PUT/DELETE` | `/api/users/...` | Διαχείριση χρηστών, ανάλογα με τον ρόλο |

Στο `POST /api/courses/snapshot` εφαρμόζεται rate limit 20 αιτημάτων ανά 15 λεπτά. Στο login και στην εγγραφή εφαρμόζονται αυστηρότερα όρια.

### Μοντέλο δεδομένων

#### User

- `username` (μοναδικό)
- `name`
- `email` (μοναδικό, προαιρετικό)
- `role` (`ADMIN` ή `MEMBER`)
- `isActive`
- `hashedPassword`
- timestamps

#### Course

- `name`
- `schoolYear`
- `teacherId` (αναφορά στον χρήστη)
- `students[]`
- `lastUploadAt`
- timestamps

Ο συνδυασμός `teacherId + name + schoolYear` είναι μοναδικός.

#### Student μέσα σε Course

- `name`
- `finalGrade`
- `estimatedExamCount`
- `criteria[]`, όπου κάθε κριτήριο έχει `name` και `grade`

Οι μαθητές και τα κριτήρια αποθηκεύονται ως embedded documents μέσα στο μάθημα και δεν διαθέτουν ξεχωριστά MongoDB IDs.

### Αλγόριθμος ενημέρωσης snapshot

Για νέο μάθημα, κάθε μαθητής ξεκινά με `estimatedExamCount = 1` αν υπάρχει έστω ένας βαθμός κριτηρίου, διαφορετικά με `0`.

Για υπάρχον μάθημα:

1. Οι παλιοί μαθητές ευρετηριάζονται με κανονικοποιημένο όνομα.
2. Τα παλιά και νέα κριτήρια συγκρίνονται με βάση το όνομα του κριτηρίου.
3. Διαφορά σε βαθμό, προσθήκη ή αφαίρεση κριτηρίου θεωρείται αλλαγή.
4. Αν υπάρχει αλλαγή, το `estimatedExamCount` αυξάνεται κατά ένα.
5. Νέοι μαθητές αρχικοποιούνται όπως στο πρώτο snapshot.
6. Το τρέχον array μαθητών αντικαθίσταται και ενημερώνεται το `lastUploadAt`.

### Αλγόριθμος προτεραιοποίησης

Σε ψευδοκώδικα:

```text
score = 0

αν estimatedExamCount == 0 ή δεν υπάρχει κανένας βαθμός κριτηρίου:
    score += 1000
αλλιώς:
    score += 100 για κάθε κριτήριο χωρίς βαθμό
    score -= estimatedExamCount * 10

αν finalGrade υπάρχει και finalGrade < 5:
    score += (5 - finalGrade) * 20

ταξινόμηση κατά score φθίνουσα
τυχαία σειρά μεταξύ ίσων score
```

Η απόκριση περιλαμβάνει επίσης επεξηγηματικές σημειώσεις (`notes`), όπως «δεν έχει εξεταστεί», «δεν έχει εξεταστεί στο κριτήριο Χ» και «χρειάζεται βελτίωση».

### Ρυθμίσεις περιβάλλοντος και εκτέλεση

Απαιτούνται κατ’ ελάχιστο οι παρακάτω μεταβλητές περιβάλλοντος:

#### Backend

```dotenv
MONGODB_URI=mongodb://...
JWT_SECRET=change-me
BACK_END_PORT=3019
```

Το `BACK_END_PORT` είναι προαιρετικό και έχει προεπιλογή `3019`.

#### Frontend

```dotenv
VITE_BACKEND_URL=http://localhost:3019
```

Εγκατάσταση και εκτέλεση ανάπτυξης:

```bash
cd backend
npm install
npm run dev

# Σε δεύτερο terminal
cd frontend
npm install
npm run dev
```

Έλεγχος production build:

```bash
cd backend && npm run build
cd frontend && npm run build
```

### Τεχνικοί περιορισμοί και σημεία προσοχής

- Δεν υπάρχει αυτοματοποιημένο test suite στο frontend και δεν υπάρχουν αρχεία tests του αλγορίθμου στο τρέχον repository.
- Το frontend URL των course endpoints είναι hard-coded για τοπική εκτέλεση και χρειάζεται ενοποίηση με τη μεταβλητή περιβάλλοντος για διαφορετικό deployment.
- Το CORS επιτρέπει σήμερα οποιοδήποτε origin (`*`). Σε παραγωγή θα πρέπει να περιοριστεί στα επιτρεπόμενα domains.
- Το JWT αποθηκεύεται σε `localStorage`, άρα απαιτείται ιδιαίτερη προσοχή στην προστασία από XSS.
- Το API δέχεται JSON έως 100 KB, επομένως πολύ μεγάλα τμήματα ή πολλά κριτήρια μπορεί να υπερβούν το όριο.
- Η τυχαία επίλυση ισοβαθμιών σημαίνει ότι δύο διαδοχικές κλήσεις μπορεί να επιστρέψουν διαφορετική σειρά για μαθητές με ίδιο score.
- Η ορθότητα της εκτίμησης εξετάσεων εξαρτάται από τη συνέπεια των ονομάτων μαθητών και κριτηρίων στα αρχεία Excel.

